import { StubPage } from "@/components/regalia/StubPage";
import bg from "@/assets/membership-key.jpg";

const Memberships = () => (
  <StubPage
    eyebrow="The Inner Circle"
    title="REGALIA"
    italic="Privileges"
    description="A membership of restraint. Recognition that travels with you, across the collection and beyond."
    bgImage={bg}
  />
);

export default Memberships;
