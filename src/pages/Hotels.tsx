import { StubPage } from "@/components/regalia/StubPage";
import bg from "@/assets/offer-suite.jpg";

const Hotels = () => (
  <StubPage
    eyebrow="The Collection"
    title="Hotels"
    italic="& Palaces"
    description="A discreet roster of palaces, residences and grand hotels — held to a single, unspoken standard."
    bgImage={bg}
  />
);

export default Hotels;
