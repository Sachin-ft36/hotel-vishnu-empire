import { StubPage } from "@/components/regalia/StubPage";
import bg from "@/assets/offer-dining.jpg";

const Offers = () => (
  <StubPage
    eyebrow="Privileges"
    title="Latest"
    italic="Offers"
    description="A season of considered indulgences across the collection — by invitation, never by spectacle."
    bgImage={bg}
  />
);

export default Offers;
